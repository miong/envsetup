import { ConfigService, AppService, BaseTabComponent, LogService } from 'terminus-core';
export declare class TitleControlService {
    private app;
    private config;
    private logger;
    private knownTabs;
    private lastUsedReplacePattern;
    private compiledReplacePattern;
    constructor(app: AppService, config: ConfigService, log: LogService);
    init(): void;
    onTabsChange(): void;
    onTabChange(tab: BaseTabComponent): void;
    processTabTitle(tab: BaseTabComponent, tabState: TabState): Promise<void>;
    getCompiledReplacePattern(): Function | null;
}
declare class TabState {
    ignoreNext: boolean;
    actualTitle: string;
    processedTitle: string;
    constructor(tab: BaseTabComponent);
}
export {};

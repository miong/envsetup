import { ConfigProvider } from 'terminus-core';
export declare class TitleControlConfigProvider extends ConfigProvider {
    defaults: {
        titleControl: {
            prefix: any;
            suffix: any;
            replacePattern: any;
            removePattern: any;
        };
    };
}

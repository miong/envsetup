import { ConfigProvider } from "tabby-core";
export declare class TrzszConfigProvider extends ConfigProvider {
    defaults: {
        trzszPlugin: {
            defaultUploadPath: string;
            defaultDownloadPath: string;
            enableDragUpload: boolean;
        };
    };
}

import { ConfigService, HotkeysService } from "tabby-core";
import { ElectronHostWindow, ElectronService } from "tabby-electron";
import { TerminalDecorator, BaseTerminalTabComponent } from "tabby-terminal";
export declare class TrzszDecorator extends TerminalDecorator {
    private config;
    private hostWindow;
    private electron;
    private cancelEvent;
    constructor(config: ConfigService, hotkeys: HotkeysService, hostWindow: ElectronHostWindow, electron: ElectronService);
    attach(terminal: BaseTerminalTabComponent<any>): void;
    private attachToSession;
}
